# --- Dependencies ---
node_modules
*/node_modules

# --- Production Build ---
dist
dist-ssr
build
*.local

# --- Logs ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# --- Environment Variables (CRITICAL) ---
# This ignores .env in the root and ALL subfolders
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Specific folder targeting (Just to be double safe)
backend/.env
backened/.env
frontend/.env

# --- OS & Editor Files ---
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# --- Uploads ---
# Even though we use Cloudinary now, ignore this folder 
# in case you test locally so images don't get to GitHub
uploads/
backend/uploads/
backened/uploads/
